# 供应链安全对照样本

这些目录是用于比赛演示的最小化、人工构造样本，覆盖正常组件、需人工复核的管理能力，以及安装钩子、下载执行、名称仿冒、依赖混淆、凭据外传和编码载荷执行。

扫描器不会执行样本代码。不要手工运行 `sc-attack-*` 对应目录中的脚本；其中网络域名使用 `.test` / `.example` 保留测试域，且只用于静态证据提取。

标签和预期安装门禁定义在 `manifest.json`。该小型数据集用于证明机制和复现流程，不用于声称生产环境准确率。
